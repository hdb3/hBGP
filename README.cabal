The base cabal file is hbgp.cabal.
There are two cabal files in the subdirectory cabal_files/.
The alternate one, hbgp-extras.cabal, can be used to build the full set of applications.
Use it by copying into the root directory over the default version.